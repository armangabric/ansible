- name: My first play
  hosts: pi_servers
  tasks:
   - name: Ping my hosts
     ansible.builtin.ping:
  
   - name: scripts folder test
      ansible.builtin.file:
        path: ~/scripts2
        state: directory

        - name: Print message
     ansible.builtin.debug:
      msg: Hello world
      
