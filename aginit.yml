- name: Update all Debian/Ubuntu packages to their latest version
  hosts: New_Devices
  become: true
  become_user: root
  tasks:
  
      - name: install btop
        ansible.builtin.apt:
        name: btop
        update_cache: true
